import { describe, expect, it } from "vitest";

// Basic sanity check to ensure the Vitest suite runs successfully

describe("sanity check", () => {
  it("verifies the test runner is configured", () => {
    expect(true).toBe(true);
  });
});
